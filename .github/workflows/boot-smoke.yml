name: Release boot smoke

on:
  push:
    tags:
      - "**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  boot-smoke:
    name: real QEMU boot smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Zig via ghr
        uses: cataggar/ghr/actions/install@v0.6.6
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tools: |
            cataggar/zig@v0.16.0 RWSGOq2NVecA2UPNdBUZykf1CCb147pkmdtYxgb3Ti+JO/wCYvhbAb/U

      - name: Install QEMU + OVMF
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends qemu-system-x86 ovmf

      - name: Cache Azure Linux ISO fixture
        id: cache-iso
        uses: actions/cache@v4
        with:
          path: fixtures/azurelinux-4.0-x86_64.iso
          key: azurelinux-4.0-x86_64-iso-v1

      - name: Download Azure Linux ISO fixture
        if: steps.cache-iso.outputs.cache-hit != 'true'
        run: |
          mkdir -p fixtures
          curl -fL --retry 3 -o fixtures/azurelinux-4.0-x86_64.iso https://aka.ms/azurelinux-4.0-x86_64.iso

      - name: Build minimal OCI fixture
        run: zig build oci-fixture -- minimal fixtures/oci-minimal

      - name: Install systemd-boot-efi (UKI stub source)
        continue-on-error: true
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends systemd-boot-efi

      - name: Build UKI-stub OCI fixture
        continue-on-error: true
        run: |
          stub=/usr/lib/systemd/boot/efi/linuxx64.efi.stub
          if [ -f "$stub" ]; then
            zig build oci-fixture -- uki-stub fixtures/oci-uki-stub "$stub"
            echo "MIZ_BOOT_TEST_UKI_OCI=${GITHUB_WORKSPACE}/fixtures/oci-uki-stub" >> "$GITHUB_ENV"
          else
            echo "::warning::$stub not found (systemd-boot-efi install failed?); MIZ_BOOT_TEST_UKI_OCI won't be set, so the UKI boot-smoke test will skip"
          fi

      - name: Install ISO/squashfs extraction tools
        continue-on-error: true
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libarchive-tools

      - name: Build verity-capable initramfs OCI fixture
        continue-on-error: true
        run: |
          initramfs=fixtures/verity-initramfs.img
          if scripts/ci/build-verity-initramfs-fixture.sh fixtures/azurelinux-4.0-x86_64.iso "$initramfs"; then
            kver=$(cat "${initramfs}.kver")
            zig build oci-fixture -- verity-initramfs fixtures/oci-verity-initramfs "$initramfs" "$kver"
            echo "MIZ_BOOT_TEST_VERITY_OCI=${GITHUB_WORKSPACE}/fixtures/oci-verity-initramfs" >> "$GITHUB_ENV"
          else
            echo "::warning::build-verity-initramfs-fixture.sh failed; MIZ_BOOT_TEST_VERITY_OCI won't be set, so the --verity boot-smoke test will skip"
          fi

      - name: Real TDNF/dracut customization and QEMU boot
        env:
          MIZ_RUN_REAL_PACKAGE_BOOT: "1"
          MIZ_BOOT_TEST_ISO: ${{ github.workspace }}/fixtures/azurelinux-4.0-x86_64.iso
          MIZ_BOOT_TEST_OCI: ${{ github.workspace }}/fixtures/oci-minimal
        run: zig build test-unsafe-chroot-real-boot --summary all

      - name: zig build test-boot-smoke
        env:
          MIZ_BOOT_TEST_ISO: ${{ github.workspace }}/fixtures/azurelinux-4.0-x86_64.iso
          MIZ_BOOT_TEST_OCI: ${{ github.workspace }}/fixtures/oci-minimal
          # Regenerating the 4 GiB LiveOS ISO consistently exceeds Zig's fixed
          # 15-minute per-test watchdog on hosted runners. The real package,
          # dracut, and QEMU integration above remains the release boot gate.
          MIZ_BOOT_TEST_SKIP_BUILD_ISO: "1"
        run: zig build test-boot-smoke --summary all
